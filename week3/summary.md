# Week 3 Summary — NumPy and Pandas for Data Manipulation

- **Topics learned:** NumPy arrays and broadcasting, Pandas DataFrame and Series, indexing, selection, `groupby` and aggregation, handling missing values.
- **Hands-on:** `numpy_pandas_examples.py` shows basic array ops and DataFrame indexing; `data_aggregate.py` cleans `sample_data.csv` and computes per-city averages.
- **Client project:** remove duplicates, coerce numeric columns, drop missing temps, then compute averages and counts.


